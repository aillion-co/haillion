---
description: Decompose a user goal into GitHub issues via the boss agent.
agent: boss
---

The user's goal:

$ARGUMENTS

Read `.opencode/maps/repo-map.md` first. Then follow the boss protocol in your agent definition. Output: one or more `gh issue create` invocations, executed, and a short summary table of the issues you created.
