---
description: Pick the next ready frontend issue and implement it.
agent: frontend
---

Run your standard loop. Pick the next available issue with label `agent:frontend` + `status:planned` whose dependencies are closed. Implement it end-to-end in `web/<app>/` using Bun + SvelteKit and open a PR.
